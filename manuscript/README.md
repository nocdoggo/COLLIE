# COLLIE manuscript, IEEE UV 2026 initial draft

This directory holds the first complete draft of the COLLIE paper. **Every
experimental result is a placeholder.** The method, the audited-benchmark
findings, the arm ladder and the claim discipline are written; the sweeps are
still running, so each missing number sits in a `\pending{}` slot whose shape
matches the result table that will fill it.

```
main.tex        IEEEtran conference class; inputs sections/* in order
sections/       one .tex per section
figures/        fig1_pipeline.tex (Figure 1, TikZ), fig2_frontier.tex (prepared)
refs.bib        34 verified entries; 16 cited in this draft
check.py        structural checks, no TeX needed
Makefile        make / make check / make check-pages
CLAIMS.md       every substantive claim -> its evidence -> its status
README.md       this file
```

---

## 1. Venue facts

Verified against the live call for papers on **2026-09-17** at
<https://universalvillage.org/ieee-uv2026/call-for-paper-4/> (HTTP 200, no
redirect). Each fact below was read off that page; the second column says where
it also appears locally.

| Fact | Value | Corroboration |
|---|---|---|
| Conference | 8th IEEE International Conference on Universal Village (IEEE UV 2026) | CFP; `~/Overleaf/IEEE_COLLIE/README.md` |
| Dates, mode | October 17 to 20, 2026, virtual and local | CFP |
| Theme | "Human-Centered AI Transformation for Future Empowerment: Explainable & Human-Intelligible Understanding, Intent-Aware Reasoning & Accountable Decision-Making, Human-Authorized AI Action, and Human-Supervised Reflective Learning" | CFP (the local scaffold truncates it at the colon) |
| Regular paper length | **5 to 8 pages** | CFP: "REGULAR PAPERS (5-8 pages)" |
| Short paper length | 3 to 4 pages | CFP: "SHORT PAPERS (3-4 pages)" |
| Template | IEEE two-column conference template | CFP; `\documentclass[conference]{IEEEtran}` |
| Paper size | US Letter, explicitly "not A4" | CFP |
| Separate abstract | 100 to 300 words, submitted through EasyChair | CFP |
| Submission system, deadline | EasyChair, **September 25, 2026** | CFP |
| Other dates | portal opened March 25, 2026; notification October 5, 2026; final manuscripts October 15, 2026 | CFP |
| Indexing | submitted for inclusion in IEEE Xplore | CFP |
| Formatting asks | remove running headers and footers, page numbering, and blue underlining on URLs and email addresses | CFP |
| **Blind review** | **Not blind.** | see below |

**Blind review, resolved rather than assumed.** A search for *blind*,
*anonymous* and *anonymised* returned zero hits across the live CFP page, the
venue's annotated sample (`conference_paper_instructions.tex` and its PDF), and
every file of the local scaffold. The venue distributes no anonymous style file,
and its own sample ships a full `\author` block with affiliations and emails.
The draft therefore uses the non-anonymous author form. The author names
themselves are `\pending{}` because the author list is the submitting authors' to
set, not this draft's.

**Assumptions used where a fact was not stated.** The CFP does not mention
over-length pages, page charges, or an appendix allowance, so the draft treats 8
pages *including references* as a hard limit and carries no appendix. `make
check-pages` fails if the built PDF exceeds 8 pages.

**Formatting asks, honoured.** IEEEtran's conference mode emits no running
header and no page number, this file adds no `fancyhdr` block, and `hyperref` is
loaded with `[hidelinks]` so no URL or address is underlined or coloured. The
venue's own sample installs a page-1 copyright footer with the ISBN string
`979-8-3195-2714-1/26/$31.00 (c)2026 IEEE`; that block is **not** in this draft
and should be added at camera-ready time if the venue requires it, since the CFP
text asks for footers to be removed and the two instructions conflict.

---

## 2. Build status

The machine this draft was written on has a full TeX toolchain, so the build was
run rather than assumed:

```
pdfTeX 3.141592653-2.6-1.40.29 (TeX Live 2026)
latexmk 4.88 (9 March 2026)
IEEEtran.cls, IEEEtran.bst  present in texmf-dist
```

```bash
make              # -> main.pdf, 8 pages
make check        # structural checks, no TeX needed
make check-pages  # page count against the venue limit
```

Last clean build: **8 pages**, US Letter (612 x 792 pt), zero undefined
references, zero undefined citations, zero LaTeX warnings on the final pass, and
exactly one overfull box, 5.4 pt, in the title block. `make check` exits 0 and
`make check-pages` passes.

`main.pdf` is tracked on purpose, so a reader without TeX Live 2026 can read the
draft without building it. Every other build product is ignored by
`manuscript/.gitignore` and is regenerated by `make`.

---

## 3. Placeholder inventory

`python3 check.py pending` prints this table and its total. The total below must
equal that output exactly.

| File | `\pending` | `\pcell` | What is missing |
|---|---:|---:|---|
| `main.tex` | 3 | 0 | author list, affiliation, email |
| `sections/abstract.tex` | 1 | 0 | headline result sentence |
| `sections/intro.tex` | 1 | 0 | nothing numeric; the marker is the standing note that every result cell is pending |
| `sections/triggers_alerts.tex` | 1 | 0 | inter-rater agreement, templates pruned for leakage |
| `sections/experiments.tex` | 2 | 0 | activation ablation; null-audit calibration and fragility; content against timing; efficiency and frontiers; operational, hypothesis and traced-case tables |
| `sections/ladder_accounting.tex` | 0 | 51 | Table I: 15 arms and controls by three endpoints (45 cells), plus 3 contrasts by two endpoints (6 cells) |
| `sections/limitations.tex` | 2 | 0 | headroom band under the integrated pipeline; integration status, pilot verdict, frozen registration hash |
| `sections/conclusion.tex` | 1 | 0 | final result paragraph |
| `figures/fig2_frontier.tex` | 1 | 0 | the reward-against-compute frontier |
| **Total** | **12** | **51** | **63 markers** |

`sections/background.tex`, `setup.tex`, `method_spec.tex`, `method_compile.tex`,
`method_verify.tex` and `related.tex` carry no placeholders: everything in them
is either established today or cited.

**`figures/fig2_frontier.tex` is prepared but not currently `\input`.** It is a
complete pgfplots skeleton with a `filecontents` data block, one placeholder row
per arm, and a header comment describing the finished figure. It was left out of
the layout to fit the 8-page limit. Re-enable it with one line in
`sections/experiments.tex` (`\input{figures/fig2_frontier}`) and remove roughly
half a column of prose elsewhere.

---

## 4. What the 8-page limit cost, and what it can buy back

The draft was written long and then compressed. Two display items were folded
into prose to fit, and both are recoverable:

1. **The per-family validity table** is preserved verbatim as a commented block
   in `sections/method_verify.tex`, immediately after the prose that now carries
   its content. Uncomment it for any longer version.
2. **The reward-against-compute frontier** is `figures/fig2_frontier.tex`, as
   above.

Two presentational decisions were improvements rather than losses. The arm
ladder and the main result table were **merged** into one full-width table, so
each rung's objection sits on the same line as the number that answers it.
Figure 1 dropped its ladder side panel once that table carried the same eleven
rungs.

---

## 5. Reference verification

Every entry in `refs.bib` was verified online on **2026-09-17**: a page showing
the title, the authors and the venue or year was fetched for each one. Nothing
is present that could not be verified, and no field was guessed. Where a page
range or a venue does not exist yet, the field is omitted rather than invented.
34 entries are verified; 16 are cited in this draft, and the rest are kept for a
longer version. `python3 check.py refs` lists the uncited ones.

Three identifiers carried in the project's design documents were checked and
one is wrong:

- **InventoryBench is arXiv:2602.12631**, "AI Agents for Inventory Control:
  Human-LLM-OR Complementarity", Baek, Fu, Ma and Peng. The design doc's id is
  correct. The upstream repository README's own BibTeX says `year={2025}` and
  keys it `baek2025ai`, but the arXiv record shows first submission 13 February
  2026. `refs.bib` uses **2026** to match the verified record; the upstream
  BibTeX is stale.
- **`InstructMPC` is arXiv:2504.05946**, not the 2512.05876 the design doc
  gives. That id resolves to a different, later paper by overlapping authors,
  now titled "Context-Aware Model Predictive Control for Microgrid Energy
  Management via LLMs". The entry is not cited in this draft.
- **The event-triggered-invocation title was truncated** in the design doc. Its
  full title ends "...in Streaming Systems", it has a single author, and it is
  accepted to the ECML PKDD 2026 Research Track with code at
  <https://github.com/GeoffreyWang1117/event-triggered-llm-streaming>.

Spot-verified facts behind sentences in the paper:

- The 4.92 percent figure is in the abstract of Kesavan, Kushwaha and Steele,
  *Management Science* 72(1):119-127, 2026, DOI `10.1287/mnsc.2024.06321`. Note
  the year of record is 2026, not the 2025 of the Article-in-Advance.
- The 21-day figure is in the abstract of Carreras-Valle and Ferrari, *AEA
  Papers and Proceedings* 115:618-623, 2025, DOI `10.1257/pandp.20251089`.
- **InvEvolve has no public code artifact.** A GitHub repository search returns
  zero repositories of that name, the paper carries no code-availability
  statement, and a coauthor's own curated index lists only the arXiv link. The
  paper says so and does not reimplement it.

One bibliographic abbreviation was applied for length: the Stockpyl chapter's
`booktitle` is given as "Tutorials in Operations Research" rather than the full
INFORMS volume subtitle.

---

## 6. Source material, and where the numbers came from

The draft was written against the repository, not against its prose. Every
number in the paper traces to `CLAIMS.md`. Two notes on provenance:

**The `collect_res/` result schemas.** The task brief pointed at
`~/Overleaf/ICLR_COLLIE/collect_res/`, described as 23 result-table CSVs plus a
README. **That folder no longer exists**: `~/Overleaf/ICLR_COLLIE` was removed
and replaced by a clone of the IEEE Overleaf project. The generator that built
it survives verbatim in shell history, so all 23 schemas were recovered and used
as the table skeletons here. The recovered index also settles the confirmatory
contrast set as arm 10 against arms 8, 6 and 11, which is what this draft uses.
The folder name says ICLR for historical reasons only; the venue is IEEE UV.

**Two whitelisted numbers were dropped after checking them.** The brief listed
"~1063 tests" and "100% coverage on shipped modules" as statable. Live
collection at `HEAD` returns exactly **1063** tests, so that figure is right,
but the project configures **no coverage gate at all** and the only coverage
artifact on disk measures one file of the 61 under `collie/`. No coverage
percentage is substantiable, so neither the test count nor any coverage figure
appears in the paper. `CLAIMS.md` records both decisions.

---

## 7. Open questions whose answers would change the draft

1. **The running example's family number.** The brief says "family 5 transit
   pause". In the frozen code, family 5 is `shipment_loss` and `transit_pause`
   is the supply leg of family 6, `compound`
   (`collie/data/families/base.py:173-180`). The draft uses family 5, a
   lost-shipment burst, which keeps the brief's family number and its
   supply-disruption character. Switching the running example to the compound
   family would change Sections 4 through 7.
2. **The third confirmatory contrast.** The recovered result index and the brief
   both name arm 11. Design doc 03\_5 §6.3 names telemetry-only adaptive OR,
   arm 2, instead. This draft follows the index and the brief, presents arm 11
   as C3, marks C3 as a benchmark rather than a mechanism contrast, and keeps
   arm 2 as the registered secondary comparator. Nothing is frozen yet, so this
   is reversible in one paragraph.
3. **Whether the page-1 IEEE copyright footer is required.** The CFP asks for
   footers to be removed; the venue's own sample installs one. Resolve before
   submission.
